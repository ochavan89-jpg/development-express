# ============================================
# Development Express — Backend Environment
# Copy to .env and fill your values
# ============================================

NODE_ENV=development
PORT=5000

# ── Supabase PostgreSQL ──
# You can provide DATABASE_URL or the individual DB_* fields below.
DATABASE_URL=
DB_HOST=db.bqvbfjweftlyjxuyol.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=YOUR_SUPABASE_PASSWORD_HERE

# ── JWT Auth ──
JWT_SECRET=devexpress_om_chavan_2026_secure_key_change_this
JWT_EXPIRE=7d

# Enable only for local demos that intentionally use hardcoded demo credentials.
ENABLE_DEMO_MODE=false

# ── CORS: add your Netlify domain ──
CORS_ORIGIN=http://localhost:5173,https://developmentexpress.in

# ── Business Logic ──
MIN_WALLET_BALANCE=5000
FUEL_ALERT_THRESHOLD=30
COMMISSION_RATE=0.15
BCRYPT_SALT_ROUNDS=10

# ── Rate Limiting ──
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# ── Company Info ──
APP_NAME=Development Express
CONTACT_EMAIL=om.chavan2026@zohomail.in
CONTACT_PHONE=9766926636
